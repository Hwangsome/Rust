// 这里先把“集成测试通常放在哪”讲清楚。
// 真正的最小示例放在 `tests/smoke.rs`，这样读者能直接看到目录位置。
pub fn run() {
    println!("== Integration Testing ==");
    println!("集成测试通常放在 crate 根目录下的 tests/ 目录中。");
    println!("当前章节提供了 tests/smoke.rs 作为最小示例。");
    println!();
}
