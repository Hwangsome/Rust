// 这一章的练习重点是：不要只会“声明类型”，还要会让类型表达真实含义。
pub fn run() {
    println!("== Lab ==");
    println!("1. 给一个 struct 补一个 &self 方法和一个 &mut self 方法");
    println!("2. 写一个 enum，并至少处理三个分支");
    println!("3. 分别写一个 Option、Result 和 HashMap 的最小示例");
    println!("4. 用 match、if let、while let 各写一个模式匹配例子");
    println!("5. 写一个带解构参数的函数");
    println!("6. 写一个链式调用例子，并观察返回 Result 后链式风格如何变化");
    println!();
}
