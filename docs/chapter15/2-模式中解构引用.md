# 2. 模式中解构引用

- **`&x`** 在模式里：解开引用，等价于 `let x = *r`
- **`ref y`** 在模式里：创建引用，等价于 `let y = &v`

最常见触发位置：`match`、`if let`、函数参数、`for`。

## 对应代码

- [topic_02_destructing_references.rs](../../chapters/chapter15/src/topic_02_destructing_references.rs)
