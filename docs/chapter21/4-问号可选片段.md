# 4. 宏里的 `?` 可选片段

宏匹配器里的 `$(...)?` 表示"这个片段可选"——0 或 1 次。
**与运行期错误传播 `?` 同符号不同义**。

## 对应代码

- [topic_04_question_mark_operator.rs](../../chapters/chapter21/src/topic_04_question_mark_operator.rs)
