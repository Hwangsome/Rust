# 3. 生命周期边界 `'a: 'b`（outlives bound）

语法 `'a: 'b` 读作 `'a` outlives `'b`（`'a` 活得至少和 `'b` 一样久）。

常见于函数签名里让多个生命周期形成"偏序关系"。

## 对应代码

- [topic_03_background_for_variance_lifetime_bounds.rs](../../chapters/chapter16/src/topic_03_background_for_variance_lifetime_bounds.rs)
