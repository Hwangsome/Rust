// `lab.rs` 不直接给答案，而是提醒读者如何自己动手验证这一章的主题。
// 这里的题目顺序基本跟随 `main.rs` 的运行顺序，方便边看边练。
pub fn run() {
    println!("== Lab ==");
    println!("1. 用 cargo 运行 chapter01，并观察每个主题的输出顺序");
    println!("2. 修改 first_program.rs，让第一个程序输出自己的名字");
    println!("3. 在 variables.rs 里补一个 shadowing 示例并重新运行");
    println!("4. 在 functions_and_code_blocks.rs 里新增一个返回值函数");
    println!("5. 在 conditionals_control_flow_and_loops.rs 里补一个新的 for range 例子");
    println!("6. 在 comments_and_printing.rs 里尝试不同的 println! 格式字符串");
    println!("7. 写一个 const 和一个 static，并比较它们的使用位置");
    println!("8. 手动制造一个类型错误，再用 rustc --explain 查看错误码说明");
    println!("9. 写出一个需要括号才能得到不同结果的表达式");
    println!();
}
