# 1. 孤儿规则 (Orphan Rule)

`impl Trait for T` 要合法，Trait 或 T 至少有一个必须在你的 crate 里。

绕开：**newtype 模式**——把外部类型包一层自己的 struct。

## 对应代码

- [topic_01_orphan_rule.rs](../../chapters/chapter17/src/topic_01_orphan_rule.rs)
