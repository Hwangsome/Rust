// 这一章的练习更偏“结构设计”和“工程组织”，不是单纯语法练习。
pub fn run() {
    println!("== Lab ==");
    println!("1. 画出 package、crate、module 三层关系图");
    println!("2. 自己创建一个模块，并从 main.rs 调用它");
    println!("3. 试着把一个函数设为私有，再观察模块外是否还能访问");
    println!("4. 给一个 pub struct 保留私有字段，并提供 new() 构造函数");
    println!("5. 写一个 Cargo.toml 依赖片段，并解释 cargo add 的作用");
    println!();
}
